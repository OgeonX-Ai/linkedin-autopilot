# Dependencies
node_modules/

# Build output
dist/

# Environment secrets — NEVER commit .env
.env
.env.local
.env.*.local

# OS / editor artifacts
.DS_Store
*.log
npm-debug.log*
.vscode/
.idea/

# Runtime data — contains live tokens and session state, never commit
.sessions.json
.users.json
.post-history.json

# AI refiner working files — may contain env vars captured during sessions
.refiner/
